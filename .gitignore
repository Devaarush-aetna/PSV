# Environment & secrets
.env
*.env
.env.*

# Claude Code settings (user-specific, may contain sensitive paths/tokens)
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# SQLite databases
*.db
*.sqlite
*.sqlite3

# Logs
logs/
*.log

# Large generated output (keep source code only)
Output/

# Scraper evidence cache and debug captures
Evidence/
evidence/
*_detail_page.html
*_detail_page.png
*_search_results.html
*_search_results.png

# Temp git staging artifacts
$path.staged

# Temporary / OS
*.tmp
~$*
.DS_Store
Thumbs.db

# Excel lock files
~$*.xlsx
~$*.xls
~$*.docx

# RunQueue state
RunQueue/

# Runtime NPPES lookup cache (auto-generated, not needed in repo)
PSV/Cache/

# Scraped PDF verification reports (generated output)
PSV/PDFS/

# Large state CSV downloads (Ohio individual provider file ~733MB)
PSV/CSVS/OH_PROVIDERS_INDIVIDUAL*.csv
lvs/adapters/scrapers/csvs/OH_PROVIDERS_INDIVIDUAL*.csv

# CSV archive cache (old dated copies)
PSV/CSVS/cache/

# PSV run output files (generated, not source)
PSV_Output_*.xlsx

# pip install version-pinning artifacts (=1.0, =2.7, etc.)
=*
