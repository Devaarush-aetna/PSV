identity:
  source_id: WY_PT
  board_name: Wyoming Board of Physical Therapy
  state: WY
  country: US
  profession_codes: [PT, PTA]
  # Wyoming Google Sheets public roster — 7-day cache.
  # Page has two Google Sheets links; scraper targets the second (nth=1).
  # Engine link_selector does not support positional nth — skipped pending selector refinement.
  base_url: "https://physicaltherapy.wyo.gov/public/lookup"
  archetype: csv_bulk

csv_bulk:
  download_strategy: google_sheet_link
  link_selector: "text=Mailing List of Active and Expired Licenses"
  download_timeout_ms: 180000    # 3-minute timeout for Google Sheets CSV export
  header_row: 3
  cache_days: 7
  cache_dir: "./PSV/CSVS"
  encoding: "utf-8-sig"
  search_columns:
    license_number: "License No."
    last_name: "Last Name"

search:
  modes:
    - mode: license_number
    - mode: last_name
    - mode: first_name
  form:
    search_by_dropdown:
      strategy: none
    search_input:
      selector: ""
    search_button:
      selector: ""
  results_wait:
    strategy: network_idle
    timeout_ms: 30000
    no_results_indicators: []

results:
  type: table
  table:
    row_selector: ""
    cell_selector: ""
    columns: {}
  has_detail_page: false
  pagination:
    enabled: false
    strategy: none

detail:
  wait:
    strategy: delay
    timeout_ms: 500
  strategies: []
  field_map:
    # Mailing List sheet (Active + Deactivated) — confirmed column names from cached CSV
    "Last Name": last_name
    "First Name": first_name
    "License No.": license_number
    "License Number": license_number
    "Date Issued": issue_date
    "Expiry Date": expiration_date
    "Expiration Date": expiration_date
    "Status": status
    # Fallback: Active-only sheet used ALLCAPS/unnamed column names
    "NAME ": last_name
    "Unnamed: 1": first_name
    "LICENSE #": license_number
    "ISSUE DATE": issue_date
    "EXPIRATION DATE": expiration_date
    "STATUS": status

output:
  status_map:
    "Active": active
    "active": active
    "Active License": active
    "Deactivated": inactive
    "deactivated": inactive
    "Inactive": inactive
    "Inactive License": inactive
    "Expired": expired
    "Suspended": suspended
    "Revoked": revoked
    # Single-letter codes from old active-only sheet
    "A": active
    "E": expired
    "I": inactive
    "S": suspended
    "R": revoked
  date_formats:
    - "%m/%d/%Y"
    - "%Y-%m-%d"
    - "%m-%d-%Y"

transport:
  browser: chromium
  headless: true
  viewport:
    width: 1280
    height: 900
  timeout_ms: 60000
  navigation_timeout_ms: 30000
  rate_limit:
    delay_between_requests_ms: 2000
    max_concurrent: 1
  retry:
    max_attempts: 2
    backoff_ms: [2000, 5000]
    retry_on: ["timeout", "network_error"]
  user_agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36"

  proxy:
    enabled: true
evidence:
  capture_html: true
  capture_screenshot: true
  capture_on: ["search_results", "detail_page", "error"]
  storage: local
  local_path: "Evidence/WY_PT/{run_id}/"

compliance:
  tos_review_date: "2026-06-11"
  requires_captcha: false
  requires_login: false
  robots_txt_compliant: true

smoke_test:
  mode: license_number
  query: "PT-1338"
  expect:
    min_records: 1
    license_number: "PT-1338"
    full_name_contains: "Wright"
    status: "active"
